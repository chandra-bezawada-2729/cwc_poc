# nginx for the CWC web interface (inside the "web" container).
# Serves the built React app and forwards /api to the backend container.
server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Fax uploads (the backend allows 25 MB per file, 50 MB per request)
    client_max_body_size 50m;

    # ── Access control (POC) ─────────────────────────────────────────────
    # One shared login in front of the whole interface. The application has
    # no user accounts yet, so without this anyone who reaches the port can
    # read every fax. "satisfy any" lets requests from inside the container
    # through unauthenticated, which is what the Docker health check uses.
    # Replace with SSO against the customer's directory before production:
    # this gives no record of WHICH person opened a document.
    satisfy any;
    allow 127.0.0.1;
    allow ::1;
    deny  all;
    auth_basic           "CBWCHC Fax";
    auth_basic_user_file /etc/nginx/.htpasswd;


    server_tokens off;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;   # the fax viewer embeds PDFs from /api
    add_header Referrer-Policy "no-referrer" always;

    location /api/ {
        proxy_pass http://backend:8080;               # service name on the internal network
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;                      # a slow fax on the local model
        proxy_send_timeout 600s;
    }

    # Hashed build assets can be cached; index.html must not be.
    location /assets/ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        # add_header in a location replaces the server-level ones, so repeat them
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header Referrer-Policy "no-referrer" always;
    }
    location = /index.html {
        add_header Cache-Control "no-cache";
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header Referrer-Policy "no-referrer" always;
    }

    # Single-page app: unknown paths serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
