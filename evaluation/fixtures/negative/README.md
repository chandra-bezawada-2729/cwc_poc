# Negative-path fixtures

Synthetic documents, no PHI, safe to commit.

| File | Purpose |
|------|---------|
| `blank_page.pdf` | White page — should produce UNKNOWN → force_manual_review |
| `degraded_scan.pdf` | ~40 % downsampled JPEG at quality=12 — unreadable scan |
| `ambiguous_quality_rx.pdf` | Payer quality-gap notice that also requests a specific prescription |
| `prescription_order.pdf` | Plain prescription order — triggers AUTO_ROUTE_DISABLED |

Generated by `make_fixtures.py` (PyMuPDF + Pillow, no live API calls).
